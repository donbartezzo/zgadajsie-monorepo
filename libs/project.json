{
  "name": "libs",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/src",
  "projectType": "library",
  "tags": [],
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/coverage/libs"],
      "options": {
        "command": "NODE_OPTIONS='--experimental-vm-modules' jest --config libs/jest.config.ts --passWithNoTests"
      },
      "configurations": {
        "ci": {
          "command": "NODE_OPTIONS='--experimental-vm-modules' jest --config libs/jest.config.ts --passWithNoTests --ci --no-warnings"
        }
      }
    }
  }
}
